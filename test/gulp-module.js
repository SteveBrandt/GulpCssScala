'use strict';

var assert = require('assert'),
    rewire = require('rewire');

xdescribe('gulp-css-scala', function() {
    it('should generate a scala file', function() {
        assert.equal(-1, [1,2,3].indexOf(4));
    });

    // ...
});
